#Number random() Method - returns a random floating point number in the range [0.0, 1.0].
#such that 0.0 <= r <= 1.0
import random
# First random number
print("random() : ", random.random())
# Second random number
print ("random() : ", random.random())